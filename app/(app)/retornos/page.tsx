import Link from "next/link";
import { requireUser } from "@/lib/auth/session";
import { listReturns } from "@/lib/operational/queries";
export default async function ReturnsPage(){const user=await requireUser();const rows=await listReturns();return <div className="space-y-4"><h2 className="text-2xl font-medium">Retornos</h2>{rows.length===0?<p className="rounded-lg border border-dashed p-6 text-sm text-muted-foreground">Nenhum retorno visível.</p>:<div className="divide-y rounded-lg border">{rows.map(r=><div key={r.id} className="flex items-center justify-between gap-3 p-4"><Link href={`/pacientes/${r.paciente_id}`}><span className="font-medium">Retorno {r.status}</span><span className="ml-2 text-sm text-muted-foreground">previsto para {r.data_prevista}</span></Link>{user.perfil!=="dentista"&&r.status==="pendente"&&<Link href={`/agenda/novo?paciente=${r.paciente_id}&retorno=${r.id}`} className="text-sm font-medium text-primary">Agendar retorno</Link>}</div>)}</div>}</div>}
